# 交付总结 — 2026-07-10 夜班

## 一句话

**语音大脑已在云端跑通（狗蛋人设、E2E 全绿），身体控制的软件栈全部就绪，固件编译完成但一个字节都没碰你的设备。**

## 今晚交付清单

### 云端已上线（EC2, 全部 systemd 自启动）
| 服务 | 作用 | 验证 |
|---|---|---|
| stackchan-xiaozhi (:8000/:8003) | 语音大脑: SileroVAD + FunASR/SenseVoice(本地) + EdgeTTS(云希) + 狗蛋人设 | E2E: 文本→LLM→TTS→485 帧 opus, 本机+CloudFront 双路 PASS |
| stackchan-litellm (127.0.0.1:4000) | OpenAI 兼容 LLM 端点 → Bedrock Claude Haiku 4.5 | 对话返回正常; OpenClaw 就位后改一行退役 |
| stackchan-goserver (:12800) | 官方开源身体 relay + 自有 RSA 密钥 | 16/16 集成测试(含 401 拒绝/帧路由/摄像头回环) |

### 公网入口（你的铁律已执行）
`da8daz4hvc7q8.cloudfront.net` → stackchan-alb(X-Origin-Verify, 默认403) → EC2。
EC2 SG 零公网服务端口, 直连 IP 超时已验证。LLM 端点仅 loopback。

### 代码（全部在本仓库, 已 push）
- `body-client/` — Python 库: 帧协议(0x01-0x1A)+RSA token+BodyClient(表情/转头/拍照/文本), 16 测试
- `happy-watcher/` — M2 状态机(去重/合并/安静时段), 8 测试
- `firmware/` — 自编译固件产物+SHA256+复现说明; secret_logic 强符号实现(mbedtls OAEP)
- `deploy/` — 全部服务配置/systemd/E2E 脚本
- `docs/operator-guide.md` — **你唯一需要看的操作文档**

## 你的设备现在什么都没变

刷机相关我只做到"编译通过+产物存档+手册"。两阶段方案:
1. **阶段一(推荐先做, 5分钟, 零风险可回滚)**: 配网页高级选项填 OTA 地址 → 语音立刻变狗蛋
2. **阶段二(想要身体控制时)**: 先 esptool 全片备份(手册有命令), 再刷; 三层回滚方案已写明

## 已知限制/待办
- LLM 暂为 Bedrock Claude 直连（这台 EC2 没找到 OpenClaw gateway; 找到后改 .config.yaml 一行）
- M2 的 Happy API 对接(轮询+解密评估)未做——等真机上身后联调才有意义
- 端到端延迟未实测北京链路; 唤醒词灵敏度属 M4 打磨范围
- 阶段一有个前提待你确认: 出厂固件配网页是否有「高级选项」(固件≥1.6.1 才有)
